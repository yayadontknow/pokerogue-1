{
  "buildCommand": "git submodule update --init --recursive && mkdir -p public/assets public/locales && cp -R assets/* public/assets && cp -R locales/* public/locales && vite build",
  "outputDirectory": "dist",
  "cleanUrls": false
}
