{
  "buildCommand": "git config --global url.\"https://github.com/\".insteadOf git@github.com: && git submodule update --init --recursive --depth 1 && vite build",
  "outputDirectory": "dist",
  "cleanUrls": false
}
