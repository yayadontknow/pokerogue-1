{
  "buildCommand": "git submodule update --init --recursive && vite build && mkdir -p dist/assets && cp -R assets/* dist/assets && mkdir -p dist/locales && cp -R locales/* dist/locales",
  "outputDirectory": "dist",
  "cleanUrls": false
}
